# SignalShift Frontend (React + Vite)

## Development
1. Install deps: `npm install`
2. Start dev server: `npm run dev`

## Backend URL
The frontend uses `VITE_API_URL` (defaults to `http://127.0.0.1:8002`).
